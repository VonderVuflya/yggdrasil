---
description: Check the Yggdrasil engine and memory status
---
Call the `ygg_health` tool and report the engine status: storage backend, whether semantic (dense) search is active, and how many memories are stored. If the engine is unreachable, tell me how to start it (`ygg start`, or `ygg doctor` to diagnose).
