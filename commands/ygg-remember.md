---
description: Save a durable memory for this project (Yggdrasil)
argument-hint: [the fact to remember]
---
Save a durable memory with the `ygg_remember` tool, scoped to the current project: $ARGUMENTS

Choose an appropriate type (decision / lesson / convention / fix / reference / project_status), keep `content` to one self-contained fact (convert relative dates to absolute), and confirm what was saved. Never store secrets, tokens, or keys.
